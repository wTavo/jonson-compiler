#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// Código generado automáticamente desde un archivo .jonson

int main() {
    int a = 10;
    float b = 36.5;
    if ((a > 5)) {
        printf("%s\n", "Valor mayor que 5");
    } else {
        printf("%s\n", "Valor menor o igual a 5");
    }
    while ((a > 0)) {
        a = (a - 1);
    }
    printf("%s\n", "Fin");
    return 0;
}